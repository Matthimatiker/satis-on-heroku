{
    "name": "{{ env.SATIS_NAME }}",
    "homepage": "https://{{ env.HEROKU_APP_NAME }}.herokuapp.com",
    "output-dir": "web",
    "require-all": true,
    "repositories": [
        {% for repository in repositories %}
            {
                "type": "vcs",
                "url": "{{ repository }}"
            }
            {% if not loop.last %}
                ,
            {% endif %}
        {% endfor %}
    ]
}
